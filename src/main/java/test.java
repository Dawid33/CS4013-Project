import booking_system.BookingSystem;

public class test {
    int totalProfit;
    
    public test(BookingSystem system) {
        // Populate fields with information from booking system.
    }

    /*
    public static void UpdateAnalytics(BookingSystem system) {
        // Update fields based on the current state of the booking system.
    }
    public void updateAnalytics() {
        // Update fields based on the current state of the booking system.
        system.getBookings();
    }
    */
}
